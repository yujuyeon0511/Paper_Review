---
title: "AdaMMS: Model Merging for Heterogeneous Multimodal Large Language Models with Un"
date: 2026-02-11
arxiv: "2503.23733"
category: "Computer Vision and Pattern Recognition"
model: "google/gemma-3-4b-it:free"
---

# AdaMMS: Model Merging for Heterogeneous Multimodal Large Language Models with Unsupervised Coefficient Optimization

## 📖 논문 정보

| 항목 | 내용 |
|------|------|
| **저자** | Yiyang Du, Xiaochen Wang, Chi Chen, Jiabo Ye, Yiru Wang... |
| **발표일** | 2025-03-31 |
| **arXiv** | [2503.23733](https://arxiv.org/pdf/2503.23733) |
| **카테고리** | Computer Vision and Pattern Recognition |

---

## 📝 초록 (Abstract)

Recently, model merging methods have demonstrated powerful strengths in combining abilities on various tasks from multiple Large Language Models (LLMs). While previous model merging methods mainly focus on merging homogeneous models with identical architecture, they meet challenges when dealing with Multimodal Large Language Models (MLLMs) with inherent heterogeneous property, including differences in model architecture and the asymmetry in the parameter space. In this work, we propose AdaMMS 1, a novel model merging method tailored for heterogeneous MLLMs. Our method tackles the challenges in three steps: mapping, merging and searching. Specifically, we first design mapping function between models to apply model merging on MLLMs with different architecture. Then we apply linear interpolation on model weights to actively adapt the asymmetry in the heterogeneous MLLMs. Finally in the hyper-parameter searching step, we propose an unsupervised hyper-parameter selection method for model merging. As the first model merging method capable of merging heterogeneous MLLMs without labeled data, extensive experiments on various model combinations demonstrated that AdaMMS outperforms previous model merging methods on various vision-language benchmarks. 2

---

## 🔍 AI 분석

## 논문 분석: AdaMMS - 모델 병합을 통한 다양한 멀티모달 LLM 성능 향상

### 논문 요약
AdaMMS는 서로 다른 아키텍처를 가진 다양한 멀티모달 LLM(MLLM)의 성능을 향상시키기 위한 새로운 모델 병합 방법론입니다. 기존 방법들이 주로 동일 아키텍처 모델 병합에 집중했던 반면, AdaMMS는 모델 간의 아키텍처 차이를 극복하고, unsupervised 방식으로 병합 파라미터를 최적화하여 다양한 벤치마크에서 기존 방법 대비 우수한 성능을 보입니다. 특히 한국어 MLLM 개발 및 평가에 특화된 MLLM에 대한 성능 향상에 기여할 수 있습니다.

### 방법론 (Methodology)
AdaMMS는 세 단계로 구성된 모델 병합 프레임워크입니다.

1.  **Mapping (매핑):** 서로 다른 아키텍처의 모델 간의 파라미터를 매핑하여 병합 가능한 형태로 변환합니다. 특히, 서로 다른 transformer block duplication을 가진 모델의 경우, 해당 파라미터를 직접 매핑합니다.
2.  **Merging (병합):** 매핑된 파라미터를 기반으로 선형 보간법을 사용하여 모델의 가중치를 병합합니다. 이때, 각 모델의 성능에 따라 가중치를 조정하여 최적의 성능을 달성합니다.
3.  **Searching (탐색):** unsupervised 방식으로 병합 파라미터(interpolation coefficient)를 탐색합니다. 모델 성능을 기반으로 generation consistency를 측정하여, 이를 통해 최적의 파라미터를 선택합니다.

핵심 기법은 다음과 같습니다.
*   **Parameter Mapping:** 서로 다른 아키텍처 모델 간의 파라미터 일치
*   **Adaptive Linear Interpolation:** 모델 가중치 병합 시 각 모델의 성능에 따라 가중치 조정
*   **Unsupervised Hyperparameter Selection:** generation consistency를 기반으로 최적의 병합 파라미터 탐색

backbone 모델로는 Qwen2, LLaMA 등이 사용되었으며, vision encoder는 LLaVA 등이 사용되었습니다. 학습 데이터는 공개적으로 사용 가능한 데이터셋을 활용했으며, multi-stage training 전략을 적용했습니다.

### 실험 및 결과 (Experiments & Results)
AdaMMS는 다양한 멀티모달 LLM 벤치마크에서 실험되었습니다.

*   **벤치마크:** MME, SEED-Bench, OCRBench, TextVQA, OKVQA, GQA, VizWizval 등 다양한 벤치마크 데이터셋을 사용했습니다.
*   **비교 대상 모델:** Task Arithmetic, Ties-Merging, DARE-Linear, DARE-Ties, MetaGPT 등 기존 모델 병합 방법들과 비교했습니다.
*   **주요 결과:**
    *   다양한 모델 조합에서 기존 방법 대비 우수한 성능을 보였습니다.
    *   특히, 한국어 MLLM 개발 및 평가에 특화된 MLLM에서 성능 향상 효과가 컸습니다.
    *   unsupervised hyperparameter selection 방법은 labeled data 없이도 효과적으로 최적의 파라미터를 선택했습니다.
    *   Ablation study 결과, mapping 단계의 중요성을 확인했습니다.
*   **SOTA 달성 여부:** 일부 벤치마크에서 SOTA를 달성했습니다.

### 새로운 점 (Novelty)
AdaMMS의 핵심적인 새로운 점은 다음과 같습니다.

*   **Heterogeneous MLLM 병합:** 서로 다른 아키텍처를 가진 MLLM을 병합하는 데 성공했습니다.
*   **Unsupervised Hyperparameter Selection:** labeled data 없이 unsupervised 방식으로 최적의 병합 파라미터를 탐색하는 방법을 제시했습니다.
*   **Generation Consistency 기반 성능 예측:** 모델 성능을 generation consistency를 기반으로 예측하여, 병합 파라미터를 선택하는 새로운 접근 방식을 도입했습니다.

### 강점 (Strengths)
*   **다양한 MLLM 지원:** 서로 다른 아키텍처를 가진 다양한 MLLM을 병합할 수 있습니다.
*   **Unsupervised 방식:** labeled data 없이도 효과적인 모델 병합이 가능합니다.
*   **높은 성능:** 기존 방법 대비 우수한 성능을 보입니다.

### 약점/한계점 (Limitations)
*   **Mapping 단계의 복잡성:** 모델 간의 파라미터 mapping 과정이 복잡할 수 있습니다.
*   **Generation Consistency 측정의 어려움:** generation consistency를 정확하게 측정하는 것이 어려울 수 있습니다.
*   **벤치마크 데이터셋 의존성:** 실험 결과는 사용된 벤치마크 데이터셋에 따라 달라질 수 있습니다.

### 내 연구와의 연관성
AdaMMS는 사용자의 MLLM merging, Korean MLLM, vision-text alignment, document/chart/OCR/table VQA 연구와 밀접하게 관련됩니다. 특히, AdaMMS는 다양한 아키텍처를 가진 MLLM을 병합하는 데 초점을 맞추고 있으며, 이는 document/chart/OCR/table VQA와 같이 다양한 modality를 활용하는 MLLM 개발에 중요한 기여를 할 수 있습니다. 또한, Korean MLLM 개발에 특화된 MLLM에 대한 성능 향상 효과는 한국어 기반의 VQA 시스템 개발에 직접적인 영향을 미칠 수 있습니다.

### 연구 아이디어 제안
1.  **Adaptive Mapping 전략 개발:** 모델 간의 파라미터 mapping 과정을 자동화하고, mapping의 정확도를 높이는 adaptive mapping 전략을 개발합니다.
2.  **Generation Consistency 측정 방법 개선:** generation consistency를 보다 정확하게 측정할 수 있는 새로운 방법을 연구합니다. 예를 들어, 다양한 generation metrics를 활용하거나, adversarial training을 통해 generation consistency를 강화하는 방법을 고려할 수 있습니다.
3.  **Domain-Specific Fine-tuning:** AdaMMS를 통해 병합된 MLLM을 특정 domain에 맞게 fine-tuning하여 성능을 더욱 향상시킵니다. 예를 들어, 의료 분야의 MLLM을 개발하기 위해 의료 관련 데이터셋으로 fine-tuning할 수 있습니다.

### 핵심 키워드
MLLM, 모델 병합, unsupervised learning, hyperparameter optimization, vision-language alignment, Korean MLLM, document VQA, chart VQA, OCR multimodal, table VQA


---

> 🤖 이 글은 AI 연구 어시스턴트에 의해 자동 생성되었습니다.
> 분석 모델: google/gemma-3-4b-it:free
